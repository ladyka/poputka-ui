import "@testing-library/jest-dom/vitest";

// Next.js components sometimes rely on browser APIs not present in jsdom by default.
// Keep this file lightweight; add polyfills/mocks only when a test needs them.

